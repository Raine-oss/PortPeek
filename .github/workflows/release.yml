name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-rust-linux:
    name: Build Linux x86_64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Build Binary
        run: cargo build --release
      - name: Rename and Compress Artifact
        run: |
          mkdir -p dist
          cp target/release/portpeek dist/portpeek-linux-x86_64
          tar -czvf dist/portpeek-linux-x86_64.tar.gz -C target/release portpeek
          cd dist && shasum -a 256 portpeek-linux-x86_64.tar.gz > portpeek-linux-x86_64.tar.gz.sha256
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: portpeek-linux-x86_64
          path: dist/*

  build-rust-macos:
    name: Build macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Build Binary
        run: cargo build --release
      - name: Rename and Compress Artifact
        run: |
          mkdir -p dist
          cp target/release/portpeek dist/portpeek-macos
          tar -czvf dist/portpeek-macos.tar.gz -C target/release portpeek
          cd dist && shasum -a 256 portpeek-macos.tar.gz > portpeek-macos.tar.gz.sha256
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: portpeek-macos
          path: dist/*

  build-rust-windows:
    name: Build Windows x86_64
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Build Binary
        run: cargo build --release
      - name: Rename and Compress Artifact
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist
          Copy-Item target\release\portpeek.exe dist\portpeek-windows-x86_64.exe
          Compress-Archive -Path target\release\portpeek.exe -DestinationPath dist\portpeek-windows-x86_64.zip
          $hash = (Get-FileHash -Path dist\portpeek-windows-x86_64.zip -Algorithm SHA256).Hash.ToLower()
          "$hash  portpeek-windows-x86_64.zip" | Out-File -FilePath dist\portpeek-windows-x86_64.zip.sha256 -Encoding ascii
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: portpeek-windows-x86_64
          path: dist/*

  publish-release:
    name: Create GitHub Release
    needs: [build-rust-linux, build-rust-macos, build-rust-windows]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Prepare Release Assets
        run: |
          mkdir -p release-assets
          cp -r artifacts/*/* release-assets/
      - name: Publish Release Assets
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
          draft: false
          prerelease: false
